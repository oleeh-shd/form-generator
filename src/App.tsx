import "./App.css";
import { Form } from "./components/form";

function App() {
    return (
        <main className="size-full bg-zinc-900 flex items-center justify-center">
            <Form />
        </main>
    );
}

export default App;
